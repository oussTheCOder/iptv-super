const i18nConfig = {
  locales: ['de', 'en'],
  defaultLocale: 'de',  // Default language is German
  prefixDefault: true    // Ensure default locale ('de') gets the /de prefix
};

module.exports = i18nConfig;
